<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Wishlist UI</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
            font-family: Arial, sans-serif;
        }
        
        body {
            background-color: #f9f9f9;
            display: flex;
            justify-content: center;
            align-items: center;
            height: 100vh;
        }
        
        .wishlist-container {
            background-color: white;
            padding: 20px;
            border-radius: 10px;
            box-shadow: 0 5px 15px rgba(0, 0, 0, 0.1);
            width: 350px;
            text-align: center;
            position: relative;
            animation: fadeIn 1s ease-in-out;
        }

        .wishlist-container img {
            width: 100%;
            height: auto;
            transition: transform 0.3s ease-in-out;
        }

        .wishlist-container img:hover {
            transform: scale(1.05);
        }

        .delete-icon {
            position: absolute;
            top: 10px;
            right: 10px;
            background-color: #c99c67;
            color: white;
            padding: 10px;
            border-radius: 50%;
            cursor: pointer;
            transition: transform 0.3s ease-in-out, background-color 0.3s;
        }

        .delete-icon:hover {
            transform: rotate(15deg) scale(1.1);
            background-color: #a07642;
        }

        .product-name {
            font-size: 18px;
            margin: 10px 0;
        }

        .price {
            font-size: 16px;
            color: gray;
        }

        .add-to-cart {
            display: inline-block;
            margin-top: 10px;
            padding: 10px 20px;
            background-color: black;
            color: white;
            text-decoration: none;
            border-radius: 5px;
            transition: transform 0.3s ease-in-out;
        }

        .add-to-cart:hover {
            transform: scale(1.1);
        }

        @keyframes fadeIn {
            from {
                opacity: 0;
                transform: translateY(-20px);
            }
            to {
                opacity: 1;
                transform: translateY(0);
            }
        }
    </style>
</head>
<body>

    <div class="wishlist-container">
       <a href="#"><div class="delete-icon">🗑️</div></a> 
        <img src="https://m.media-amazon.com/images/I/61hRy2f27pL._AC_SX679_.jpg" alt="DJI Air 3">
        <p class="product-name">DJI Air 3</p>
        <p class="price">$141748</p>
        <a href="#" class="add-to-cart">Add to Cart</a>
    </div>

</body>
</html>
